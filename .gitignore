# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Cordova
www
platforms
plugins

# iOS build output (archives, exported IPAs, xcodebuild derived data)
build

# Android signing (keep private — never commit)
build.json
keys/
*.jks
*.keystore

# fastlane — Play service-account key (secret) + generated reports
fastlane/play-service-account.json
fastlane/report.xml
fastlane/Preview.html
fastlane/*.json

# App Store Connect API key
fastlane/*.p8
fastlane/certs/
