import './ComingSoon.css'

// Standalone maintenance splash shown in place of the whole app (see shouldShowComingSoon).
const ComingSoon = () => (
  <div class="coming-soon">
    <h1 class="coming-soon-title">Coming soon...</h1>
  </div>
)

export default ComingSoon
